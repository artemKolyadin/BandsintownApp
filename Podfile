# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'TestApiRequest' do
    use_frameworks!
  # Pods for TestApiRequest
    pod 'Realm/Headers'
    pod 'Realm'

end
